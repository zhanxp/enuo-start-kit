(function() {
    'use strict';
    angular.module('app.main.home')
        .controller('homeCtrl',
            function($rootScope,
                $scope,
                $state,
                $appConfig,
                $cookies,
                mainSrv) {


            }
        );
})();